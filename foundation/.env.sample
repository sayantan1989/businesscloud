# Sample environment variables for the foundation service
# Copy this file to .env and populate with real values in your local environment.
# Do NOT commit the .env file. It is already ignored via .gitignore.

# AWS credentials (avoid committing real credentials)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# GitHub configuration
# Personal access token with minimal required scopes
GITHUB_TOKEN=
# Organization or user that owns the repositories/resources
GITHUB_OWNER=my-org

# Database connection URL (e.g., postgres://user:password@host:5432/dbname)
DB_URL=