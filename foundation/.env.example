# Example environment configuration for foundation service
# Copy this file to .env and fill in the values. Do NOT commit the .env file.

# AWS credentials (avoid committing any real credentials)
AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY
AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_ACCESS_KEY

# GitHub configuration
# Use a fine-grained token with minimal scopes if needed; never commit the token
GITHUB_TOKEN=your_github_token_here
GITHUB_OWNER=my-org

# Database connection string
# Example: postgres://user:password@localhost:5432/mydb
DATABASE_URL=postgres://user:password@localhost:5432/mydb